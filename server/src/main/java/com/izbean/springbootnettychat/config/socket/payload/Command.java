package com.izbean.springbootnettychat.config.socket.payload;

public enum Command {

    JOIN,
    ERROR,
    HAVE,
    HELO,
    SEND,
    FROM,
    LEFT,
    QUIT,
    BYE,
    NICK

}
